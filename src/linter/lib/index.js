const utils=require("../utils/utils"),unsupportedFns=require("./unsupported-fns"),unsupportedTags=require("./unsupported-tags"),wxmlLineRules=[e=>{const s="组件阿里小程序未实现";for(let u=0;u<unsupportedTags.length;u++){let l=unsupportedTags[u],r=new RegExp("<"+l+"\\s+");if(e.match(r))return{source:e,rule:[l,s].join("")}}return null}],wxmlFileRules=[],wxssFileRules=[],wxssLineRules=[],scriptLineRules=[e=>{const s="方法阿里小程序未实现";for(let u=0;u<unsupportedFns.length;u++){let l=unsupportedFns[u],r=new RegExp("\\."+l+"\\(");if(e.match(r))return{source:e,rule:[l,s].join("")}}return null}],scriptFileRules=[(e,s)=>{const u=/['"][\w\/.]+['"]/,l=e.match(/require\(([^)]+)\)/);if(l&&!u.test(l[1])){return{line:utils.calcLine(e,l[0]),path:s,source:l[0],rule:"`require`，参数只能是字符串直接量，不能是变量（如：`var path = '/a/b/c'; require(path);`）"}}return null},(e,s)=>{const u=/triggerEvent\(['"]/,l=e.match(/triggerEvent\(([^)]+)\)/);if(l&&!u.test(l[0])){return{line:utils.calcLine(e,l[0]),path:s,source:l[0],rule:"triggerEvent(name, data)`，`name`目前只支持字符串直接量，不支持变量"}}return null}];module.exports={wxmlLineRules:wxmlLineRules,wxmlFileRules:wxmlFileRules,wxssLineRules:wxssLineRules,wxssFileRules:wxssFileRules,scriptLineRules:scriptLineRules,scriptFileRules:scriptFileRules};